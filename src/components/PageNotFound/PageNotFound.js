function PageNotFound() {
  const style = {
    marginTop: "5rem",
    fontSize: "5rem",
    textAlign: "center",
  };

  return <h2 style={style}>Page Not Found</h2>;
}

export default PageNotFound;
